name: ci

on:
  push:
    branches:
      - 'master'

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - 
        name: Checkout
        uses: actions/checkout@v2
      -
        name: Set up QEMU
        uses: docker/setup-qemu-action@v1
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      # -
      #   name: Login to Aliyun Registry
      #   uses: docker/login-action@v1
      #   with:
      #     registry: registry.cn-shenzhen.aliyuncs.com
      #     username: ${{ secrets.ALINAME }}
      #     password: ${{ secrets.ALIPASS }}
      # -
      #   name: Build and push
      #   uses: docker/build-push-action@v2
      #   with:
      #     context: .
      #     platforms: linux/amd64,linux/arm64
      #     push: true
      #     tags: registry.cn-shenzhen.aliyuncs.com/test_gg/sz:3.4.13
      # #- # run tests using image from docker hub
      #   # name: Run Tests
      #   # run: make tests
      # - # copy multiplatform image from dockerhub to quay and ghcr
      #   name: Push Image to multiple registries
      #   uses: akhilerm/tag-push-action@v1.0.0
      #   with:
      #     src: k8s.gcr.io/etcd:3.4.13-0
      #     dst: registry.cn-shenzhen.aliyuncs.com/test_gg/sz:3.4.13
